apiVersion: apps/v1
kind: Deployment
metadata:
  name: "{{ .Values.spring.labels }}-deployment"
  labels:
    app: "{{ .Values.spring.labels }}"

spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: 3
  {{- end }}
  selector:
    matchLabels:
      app: "{{ .Values.spring.labels }}"
  template:
    metadata:
      labels:
        app: "{{ .Values.spring.labels }}"
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "{{ .Values.spring.service.geospatial.port }}"
        prometheus.io/path: "/actuator/prometheus"
        rollme: {{ randAlphaNum 5 | quote }}
    spec:
      terminationGracePeriodSeconds: 60
      nodeSelector:
        role: app
      containers:
        - name: "{{ .Values.spring.labels }}"
          image: "{{ .Values.spring.image.repository }}:{{ .Values.spring.image.tag }}"
          imagePullPolicy: Always
          # Ingress 라우팅 제외될 때까지 시간 벌어주기
          lifecycle:
            preStop:
              exec:
                command: [ "/bin/sh", "-c", "sleep 20" ]

          env:
            - name: SPRING_REDIS_CACHE_HOST
              value: {{ .Values.spring.redis.cache.host | quote }}
            - name: SPRING_REDIS_CACHE_PORT
              value: {{ .Values.spring.redis.cache.port | quote }}

            - name: SPRING_REDIS_STORAGE_HOST
              value: {{ .Values.spring.redis.storage.host | quote }}
            - name: SPRING_REDIS_STORAGE_PORT
              value: {{ .Values.spring.redis.storage.port | quote }}

            - name: SPRING_KAFKA_BOOTSTRAP_SERVERS
              value: {{ .Values.spring.kafka.bootstrapServers | quote }}

            - name: ZIPKIN_ENDPOINT
              value: {{ .Values.spring.zipkin.endpoint | quote }}
            - name: EUREKA_DEFAULT_ZONE
              value: {{ .Values.spring.eureka.url | quote }}

          ports:
            - containerPort: {{ .Values.spring.service.geospatial.port }}
          resources:
            requests:
              cpu: "200m"    # 0.2 vCPU
              memory: "400Mi"
            limits:
              cpu: "400m"    # 0.4 vCPU
              memory: "800Mi"

          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: {{ .Values.spring.service.geospatial.port }}
            initialDelaySeconds: 45 # Spring 뜨는 시간 고려해서 넉넉히
            periodSeconds: 10
            failureThreshold: 3

          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: {{ .Values.spring.service.geospatial.port }}
            initialDelaySeconds: 10
            periodSeconds: 10
            failureThreshold: 3
            successThreshold: 1
